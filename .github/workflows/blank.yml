name: Prueba SENAPRED

on:
  workflow_dispatch:

jobs:
  prueba:
    runs-on: ubuntu-latest

    steps:
      - name: Obtener HTML SENAPRED
        run: |
          curl -L https://www.senapred.cl/alertas | head -100
