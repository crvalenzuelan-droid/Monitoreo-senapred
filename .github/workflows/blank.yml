name: Probar SENAPRED

on:
  workflow_dispatch:

jobs:
  prueba:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar página
        run: |
          curl -L https://www.senapred.cl/alertas > senapred.html

      - name: Mostrar contenido
        run: |
          head -100 senapred.html
